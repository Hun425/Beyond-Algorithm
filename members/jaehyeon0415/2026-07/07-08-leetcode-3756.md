## [3756. Concatenate Non-Zero Digits and Multiply by Sum II](https://leetcode.com/problems/concatenate-non-zero-digits-and-multiply-by-sum-ii/description/?envType=daily-question&envId=2026-07-08)

### 접근 방법
- [3754. Concatenate Non-Zero Digits and Multiply by Sum I](https://leetcode.com/problems/concatenate-non-zero-digits-and-multiply-by-sum-i/description/?envType=daily-question&envId=2026-07-07)의 풀이를 재사용함.

### 코드
```kotlin
class Solution {
    private val mod = 1_000_000_007L

    fun sumAndMultiply(s: String, queries: Array<IntArray>): IntArray {
        return IntArray(queries.size) { index ->
            val query = queries[index]

            sumAndMultiplyNumber(
                s.substring(query[0], query[1] + 1).toLong()
            )
        }
    }

    private fun sumAndMultiplyNumber(number: Long): Int {
        var sum = 0L
        var x = 0L
        var digit = 1L
        var n = number

        while (n > 0) {
            val digitNum = n % 10

            if (digitNum != 0L) {
                sum += digitNum
                x += digitNum * digit
                digit *= 10
            }

            n /= 10
        }

        return ((sum * x) % mod).toInt()
    }
}
```

### 복잡도
- 시간복잡도:
- 공간복잡도:

### 회고
- 매우 큰 수와 처리 시간을 줄이는데 막힘
- 더 생각할 시간이 필요함